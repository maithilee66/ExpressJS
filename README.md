# ExpressJS Learning Repo

This repository is my practice space for learning Express.js. It includes:

- Handling all HTTP methods
- Using multiple middleware callbacks
- Creating custom middleware
- Structuring routes, controllers, and middleware

## How to Run
1. Clone the repo
2. Run `npm install`
3. Run `node index.js` or `npm start`

